// Defining the structure of the response data
export interface GetDataResponse {
    firstName: string;
    location: string;
    imageUrl: string;
      // You can add any other fields that your backend returns
    }